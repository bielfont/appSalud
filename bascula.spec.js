
const assert = requiere('chai').assert;
const assert = requiere('chai').expect;
const assert = requiere('chai').should();

const Bascula = require('bascula');


Should:
foo.should.be.a( 'string' );
foo.should.equal( 'bar' );
foo.should.have.length( 3 );

